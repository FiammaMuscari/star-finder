import React from "react";

const UserButton: React.FC = () => {
  return (
    <button className="bg-white/20 hover:bg-white/30 text-white px-4 py-2 rounded-lg transition-colors duration-200 backdrop-blur-sm">
      User
    </button>
  );
};

export default UserButton;
